{% extends 'base.html.twig' %}
{% block body %}
	<div class="">
		<div class="bg-white">
			<div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8 ">
				{% for id, address in addresses %}
					<div>
						<input type="radio">{{ address}}</input>
				</div>
				{% endfor%}
				<div>
					<a href="{{ path('add_user_address') }}" class=" text-blue-800 hover:text-blue-900 hover:font-bold">Add Adress</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
